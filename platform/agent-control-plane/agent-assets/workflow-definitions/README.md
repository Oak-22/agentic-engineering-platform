# Workflow Definitions

Store reusable task flows and prompt-independent execution sequences here.
Runtime prompt files may link to these definitions and add only the metadata
required for native discovery.
