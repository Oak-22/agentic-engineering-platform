# Claude Code Repository Adapter

@AGENTS.md

Claude Code discovers project skills through `.claude/skills/` and
path-specific rules through `.claude/rules/`. These adapters route to the
canonical shared assets under
`platform/agent-control-plane/agent-assets/`.
