# Skills

This directory is the GitHub Copilot discovery adapter for canonical
provider-neutral skill packages under `.agents/skills/`.

Each skill directory here is a relative symlink. Run
`scripts/check-agent-discovery-layout.sh` to verify the one-to-one mapping
across GitHub Copilot and Claude Code.
